# 落云宗企业平台 —— 开发任务书 v3（最终版）

> 品牌名：落云宗·企业 AI 管理台。UI 规格以 5 页设计图为准（`E:\下载\DSH企业级部署管理平台_1..5_*.jpg`：员工工作区 / 成员与额度 / 模型与权限 / 实例管理 / 插件管理）。
> 参照项目：https://github.com/boe1900/owndsh （BYOD 路线，借鉴其角色矩阵、Token 生命周期、网关校验模式；不采用其 BYOD 部署形态）。

## 0. 你的角色与工作方式

你是本仓库（D:\deepseek-harness）的资深贡献者。开工前先读 AGENTS.md 与 docs/architecture.md。只做本任务书划入范围的问题域。所有产品决策已在本文件拍板（第 1 节），实现中不得自行引入新的产品语义；遇到与仓库约定（AGENTS.md）冲突的点，停下来向需求方提问。

## 1. 已拍板的决定（全部，不要再问）

1. **路线：服务器集中托管。** 平台部署在企业内网服务器或云服务器，员工浏览器登录即用。一人一账号、一实例（独立 DSH_HOME + 独立 uid + 独立端口）、会话与数据隔离，同事互不可见。
2. **配额语义：硬停。** 额度花到线，自动拒绝该成员的新 LLM 请求；进行中请求放行至完成。
3. **密钥形态：** 真实 Key 只存在于 Relay 进程；实例只持虚拟钥匙（opaque token）。
4. **架构模式：** 独立控制面 + 标准 harness 插件/Provider 扩展，不改 harness 本体（agent-loop、core 包不动）；仅当扩展点不够时才进仓库加 seam。
5. **隐私边界：管理员不能看员工会话内容与内容库产物正文。** 审计只记元数据（谁/何时/哪个模型/token 数/门禁结论）；Relay 日志只记请求元数据，绝不落 prompt/completion 正文；会话日志只存在于实例本地 DSH_HOME，不上送控制面。
6. **配额周期时区：以部署服务器本地时区为准**（内网物理机与云服务器同规则），不引入 per-tenant 时区配置。
7. **UI 规格以落云宗 5 页设计图为准**，页面上每个可操作元素须能映射到第 4 节术语与第 5 节角色。

## 2. 背景与现状约束（已核实，不要重新调研）

- DSH 现为单用户、单机、本地优先；全仓无 tenant/SSO/login/quota 代码。
- 三块可依托资产：
  - credentials seam（`packages/credentials/credentials`）：llm-deepseek 每请求 `resolve` 取 key、不缓存——虚拟钥匙/Relay 的天然挂点；
  - DSH_HOME 单根数据模型（`packages/util/home-paths`）：settings/credentials/sessions/profiles/skills/outputs 全部从同一 root 派生——一人一 home 只差外层调度；
  - gateway/connection 预留的 auth 挂点（`packages/client/connection` 对 `0.0.0.0` 的禁用注释即等待认证层）。
- OwnDsh 可直接借鉴的设计：五角色矩阵、Token 生命周期（Refresh Token 持久化、Access Token 只驻内存、设备级撤销、401 续期重试一次）、网关 per-request 校验、PostgreSQL + Flyway + Redis + JWT 技术栈候选。

## 3. 目标与非目标

**目标：** 按第 7 节阶段顺序交付，本次任务先完成【阶段 0 + 阶段 1】。

**非目标（显式出界）：** SSO/LDAP 实际对接（接口留好不做实现）；计费对账/发票；跨企业 SaaS 多租户；容器编排（MVP 单机多进程）；员工间协作/会话共享；管理员查看员工内容（永久非目标，见决定 5）。

## 4. 术语表（一词一义）

- **账号**：管理端用户目录中的身份记录，含部门、角色、状态（正常/冻结/禁用）。
- **实例**：一个 DSH 进程，持独立 DSH_HOME，归属唯一账号；一实例多 session。
- **虚拟钥匙**：实例持有的 opaque token，仅用于向 Relay 自证，换不出真实 Key。
- **Relay**：管理侧进程，持真实上游 Key，per-request 校验身份/授权/配额后转发 LLM 请求；日志只记元数据。
- **真启动预检**：在隔离临时 DSH_HOME 中启动实例、跑一次完整模型 turn、确认全部插件注册成功，超时 120 秒。
- **授权集 / 实际生效集**：管理端为实例配置的模型与额度 / 实例启动后 llm 适配器实际注册的模型 + profile 组合 dump。
- **暂存 / 生效**：插件新版本先落暂存区，实例重启时生效（两阶段）。

## 5. 架构落点

| 域 | 落点 |
|---|---|
| 实例编排 | 服务端 supervisor 组件（独立于 harness）：拉起/健康检查/崩溃重启/回收空闲；uid + 隔离目录 + 端口分配 |
| 账号认证 | 独立控制面（PostgreSQL + Flyway + Redis + JWT 为技术栈候选）+ DSH 侧最小接入 |
| 密钥代理 | credentials seam 新 Provider（credentials-broker）+ Relay 进程 |
| 计量配额 | Relay per-request 强制执行（不信任实例内自律）；用量元数据落控制面库 |
| 模型权限 | 成员×模型授权矩阵 + 计价倍率（输入/输出/缓存读/缓存写四字段） |
| 插件治理 | bundle patch 层复用 + 下发通道/暂存生效/预检/回滚为本项目新增 |
| 管理控制台 | 独立 Web 应用（落云宗 UI），审计页只呈现元数据 |

工程约定：registrations 都是 effects；新 capability 一次补齐三角色；model-visible 的输入必须有 session event；非平凡改动同 PR 附 Agent Note；每个行为变更配 keyless snapshot 测试。

## 6. 试点受管插件：内容创作工作站（阶段 6 的验收载体）

仓库内已有内容创作插件组（`dsh-creator-workbench` + `client-ui-creator-workbench` + `ui-content-studio` + `content-outputs`/`content-schedule`），作为第一个纳入企业管理的插件。

架构自动覆盖、无需特判：产物/日历写在 `<DSH_HOME>/outputs`（按人隔离，管理员按决定 5 不可见正文）；LLM 用量走实例→Relay（自动计量）。

**必须先完成的移植前置（列为本域子任务）：**

a) `CREATOR_WORKBENCH_SKILLS_PATH` 的 Windows 本地默认值改为管理端 per-instance 可配置项；技能库（天龙 skills）作为服务器侧管理员统一维护的只读资产，版本由管理端管。
b) 服务器镜像补齐 pwsh + python 运行时，或将 `scan_inputs.ps1` / `illustrate.ps1` / `export_content.ps1` / `run_gate.py` 四脚本移植为跨平台实现（保留 illustrate 失败不阻塞导出的语义）。
c) 管理能力验收：全局下发到全部实例、定向下发到单个实例、暂存→重启生效、per-instance 配置下发、禁用后员工工作台入口消失。
d) 合规增强：`run_gate` 的 compliance 门禁结论上报控制面审计日志（只记结论与时间，不记内容正文——与决定 5 一致）。

## 7. 分阶段交付（验收 = 给定/当/则）

- **阶段 0 需求宪法**：术语表、角色×权限矩阵（候选五角色：平台管理员/模型管理员/插件管理员/审计员/员工，按需裁剪）。验收：5 页 UI 图上每个可操作元素映射到唯一术语与角色。
- **阶段 1 实例编排 MVP**：supervisor（拉起/健康检查/崩溃 30 秒内重启/回收空闲）+ 反向代理统一入口 + 实例页数据（端口/pid/uid/目录/内存/磁盘）。验收：一键拉起 3 实例；实例 A 工具读不到实例 B 的 workspace；kill 后自动恢复且会话可 resume。
- **阶段 2 账号认证**：登录 + JWT + 实例绑定。验收：未登录访问任意入口被拒；用户 A 的会话列表不含用户 B 的 session id。
- **阶段 3 Relay 密钥代理**：实例环境变量/配置 dump/内存无真实 Key；虚拟钥匙吊销后新请求立即被拒（传播 ≤5 秒，in-flight 放行）；换上游供应商零改实例。
- **阶段 4 计量配额**：按人/部门月度额度，硬停，服务器本地时区重置；执行点在 Relay。验收：实例内篡改本地状态不影响拦截；超限错误对员工可读。
- **阶段 5 漂移对齐**：授权集 vs 实际生效集比对。验收：人为制造差异（实例残留旧插件版本），比对页按时限标红。
- **阶段 6 插件治理（试点=内容创作工作站）**：下发协议 + 暂存生效 + 预检 + 回滚 + 第 6 节四项前置。验收：投放必然注册失败的插件，预检拦截、实例不受影响；内容插件定向下发与禁用按 6c) 项通过。

## 8. 非功能需求

规模 ≤100 员工、≤50 并发实例；单台服务器（内网或云）；实例崩溃自动重启且会话可 resume；所有新行为配 keyless snapshot 测试；隐私不变量（决定 5）写入执行的顶层 gate——构造一个含正文的场景，证明审计/Relay 日志中不存在正文。
