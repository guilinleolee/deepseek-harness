# 卡巴格管理台栏目与功能规划 v2（修订版）

日期：2026-09-25 ｜ 状态：经开源竞品调研 + DSH 架构约束评审后修订，替代 v1 规划
调研依据：Open WebUI / LibreChat / AnythingLLM / Dify / FastGPT / one-api / new-api / authentik / Logto 等开源项目 + DSH 主干（Cordis）架构核对

---

## 一、v1 → v2 修正对照（架构事实）

| v1 假设 | v2 修正 | 依据 |
|---|---|---|
| UI 以 webview 嵌入 DSH web 界面 | 独立管理台 `:8460` + 网关按 Host 反代（现状即正确方案） | `:3080` 前端是预构建 SPA，假设根路径，无路由/iframe 挂载点 |
| 部门做数据隔离（可开关谁看得见谁） | 一人一实例 + 独立 `DSH_HOME` = 物理级硬隔离（现状，强于任何逻辑隔离）；部门只承担**组织结构**与**配额分组**两个职责 | 实例编排已上线 |
| 权限拦截中间件统一拦截 WebUI 与工具调用 | 分两层：管理面 = 网关 JWT 角色门禁（已有）；实例内工具 = `ctx.tools.guard()`（权威）+ `ctx.approval`（自带审计事件）。`ctx.tools.restrict()` 不是安全边界，禁用 | `packages/core/tools/README.md` |
| DSH 核心感知用户 | 核心无用户概念（identity 仅匿名 UUID）；管理类工具（`user:*`）挂平台层，只挂管理员实例 | `packages/identity/` |
| 配额按裸 token 计量 | **额度点数 + 模型倍率 + 分组倍率**（one-api 验证模型） | 不同模型价差数十倍，裸 token 无法横向比较 |

## 二、栏目结构 v2（9 个一级栏目）

标 ✅ = 已实现 ｜ 🔨 = 本次开发 ｜ 📋 = 已规划待开发

1. **总览仪表盘** ✅🔨 — 现有总览页增强：新增告警卡（配额将尽成员数、24h 失败登录数、审计事件流摘要）。
2. **成员管理** ✅🔨 — 现有页增强：密码策略校验落地、全部操作埋审计点、列表显示最后登录时间/IP、额度列改为点数显示。
3. **部门与角色** 🔨 — 新页。部门：平铺列表（中小企业不做部门树）+ 每部门分组倍率。角色：3 固定角色（admin/auditor/employee）的权限点矩阵**展示**（本期只读展示，编辑后置），超管专职页说明。
4. **模型与供应商** ✅🔨 — 现有页扩展：每模型**倍率编辑**（模型倍率/补全倍率），未配置模型默认倍率 1。
5. **令牌与配额** 📋 — 虚拟钥匙管理独立页（本期钥匙仍随成员页管理）；配额点数总览。
6. **实例管理** ✅ — 现有页不动。
7. **安全与审计** 🔨 — 新页，两段：**审计日志**（查询/筛选/导出，只追加不可删）+ **安全设置**（登录速率限制参数、密码策略参数，落 `data/security.json` 可改）。
8. **插件管理** ✅ — 现有页不动。
9. **个人中心** 📋 — 员工侧（改密/本人用量/下线设备），后续阶段。

## 三、角色权限矩阵 v2

| 权限点 | admin | auditor | employee |
|---|---|---|---|
| 管理台登录 | ✅ | ✅（新增，本期落地） | ❌（仅实例子域） |
| 总览/实例/模型/插件页 | 读写 | **只读** | — |
| 成员列表查看 | 读写 | **只读** | — |
| 成员变更操作 | ✅ | ❌ | — |
| 审计日志查看/导出 | ✅ | **✅**（核心职责） | — |
| 安全设置修改 | ✅ | ❌（只读展示） | — |
| 实例内工具组（命令行/文件/联网） | 全开 | 按 audit 需要最小化 | 按角色权限点（阶段 9+，guard 插件落地时生效） |

## 四、审计日志设计（本期最高优先）

**存储**：`data/audit.jsonl`，只追加 JSONL，一行一事件；MVP 无轮转（量级：中小企业每年 <10 万行），提供导出归档。查询倒序读取，单次上限 500 行。

**事件格式**：
```json
{"ts":"2026-09-25T12:00:00.000Z","actor":{"account":"admin@x","role":"admin","ip":"1.2.3.4"},
 "action":"member.reset_password","target":"member-a@x","result":"ok","detail":{"key":"..."}}
```

**事件分类**（action 前缀即分类）：
- `auth.*`：login_success / login_fail / login_rate_limited / revoke_all
- `member.*`：create / update / disable / enable / reset_password / role_change / quota_change / models_change / idp_import
- `provider.*`：add / remove ｜ `model.*`：add / remove / allowlist_change / ratio_change
- `instance.*`：start / stop / restart ｜ `plugin.*`：push / activate / remove / rollback
- `quota.*`：exceeded（relay 侧上报） ｜ `audit.*`：export ｜ `security.*`：config_change

**隐私红线**：只记元数据（谁/何时/对谁/动作/结果/少量标量 detail），永不落会话正文、密码原文、密钥原文。

## 五、登录安全设计（本期）

- **速率限制**：按「账号+IP」组合滑动窗口计数（内存 Map，daemon 重启清零为已记录的 MVP 限制），默认 15 分钟内 10 次失败 → 锁 15 分钟；成功登录清零该账号计数。参数进 `data/security.json`。
- **密码策略**：长度 ≥ 8 且至少含大写/小写/数字中的三类；不得与账号名相同。应用于建号设密、重置密码（存量账号在下一次变更时生效，不强制作废现有密码）。参数进 `data/security.json`。
- 现状缺口（TLS、通配域 Cookie、刷新令牌轮换）维持生产缺口清单，本期不动。

## 六、配额模型 v2：额度点数 + 倍率（本期）

- **单位**：点。`消耗点 = (输入tokens × 模型倍率 + 输出tokens × 模型倍率 × 补全倍率) × 分组倍率`。倍率未配置时模型倍率=1、补全倍率=3（对齐主流对话模型输出/输入价格比）、分组倍率=1。
- **扣减流程**（relay 每请求）：预扣（请求体字符数÷4 估算输入 + `max_tokens`（缺省 1024）估算输出，按公式预扣）→ 响应后按实际 usage 实结（多退少补）→ 请求失败全额返还。流式沿用已注入的 `include_usage` 取终值。
- **超额行为**：月度点数耗尽 → 429 `insufficient_quota`（沿用现有硬停语义，文案改为点数）；预扣计入配额判定（usedPoints+未实结预扣 ≥ 月度点数即拒），断连请求全额返还预扣；工具调用类硬拒绝在阶段 9（实例内 guard）一并落地。
- **数据结构**：`data/usage.json` 每账号每月新增 `points` 累计（token 进/出照旧累计供展示）；`accounts.json` 每账号新增 `monthlyPoints`（缺省=不限、0=即停）；存量 `monthlyTokens` 保留为后备判据（`monthlyPoints` 未定义时按旧逻辑），控制台标注「旧制」。
- **配置**：`data/ratios.json`：`{"models":{"<model-id>":{"ratio":2,"completionRatio":3}},"defaultCompletionRatio":3,"groups":{"<部门名>":1.5}}`；模型页内联编辑、部门在部门与角色页编辑。

## 七、后续阶段（本次不开发）

- **阶段 9**：实例内工具 RBAC —— 平台下发角色→工具组映射，实例侧 guard 插件按映射在 `tools/pre-execute` 拒绝；`ctx.approval` 审批事件回流平台审计。**已实施（2026-09-25）**：`data/toolpolicy.json` 角色→deny 工具组（command/fs/network）+ `POST /console/api/toolpolicy`（审计 `toolpolicy.change`）+「部门与角色」页工具组开关；策略按实例归属账号的角色原子写 `<home>/tool-policy.json`（mtime 热生效）；实例侧守卫插件 `platform/plugins/kabage-tool-guard`（`ctx.tools.guard()`，拒绝写 `<home>/guard-events.jsonl` 桥文件，daemon 周期转写 `guard.deny` 进平台审计后清空）。`ctx.approval` 审批回流未在本期范围。
- **阶段 10**：个人中心（员工改密、本人用量、设备下线）+ 邀请注册链接。**已实施（2026-09-25）**：门户员工侧 `/me`（资料/改密/本人当月用量/最近登录+下线所有设备，admin/auditor 同样可用）+ `GET /login` 直达；邀请注册 = 成员页生成一次性链接（`data/invites.json` 只存 code 的 SHA-256，timingSafeEqual 常数时间校验）+ 公开 `/register?code=` 注册页 + `POST /api/register`（按 IP 复用登录速率限制；建号走 addAccount 同一函数，成功后同步实例 tool-policy）；审计 `member.self_password_change`/`auth.revoke_all`/`invite.create`/`invite.revoke`/`member.register`。
- **阶段 11**：2FA（TOTP）、企业微信/钉钉 SSO、邮件通知（配额告警/账号变更）。**已实施（2026-09-25，骨架+mock 验证）**：11A TOTP 全量落地（手写 RFC 4648 base32 + RFC 6238 实现，附录 B 向量验收；`data/twofa.json` 密钥 AES-GCM 加密；绑定/确认/解绑/admin 救援与审计齐备；登录两段式 `totp_required`→`/api/auth/totp`，限流同桶；`require2faRoles` 软强制不硬锁）；11B 邮件通知（`data/notify.json` off/file/smtp 三态，file 模式完整 MIME 落 outbox、smtp 模式零依赖客户端，relay 80% 告警每账号每月一次 + 建号/禁用/重置通知且不含密码原文）；11C SSO 骨架（企业微信+钉钉配置驱动，state/bindToken 一次性 5 分钟，ssoIds 绑定，mock OAuth 全流程验证）。**真实 SMTP 与真实企业微信/钉钉待配置凭证后真机验证**；2FA 硬强制属阶段 11b。
- **阶段 12**：管理类 DSH 工具（`user:list` 等）挂平台层 + 管理员实例。**已实施（2026-09-25；真实 agent 调用待真机验证）**：服务令牌（`data/service-tokens.json` 哈希存储 + `add-service-token/revoke-service-token` CLI + `Bearer kbsvc-` 认证扩展，admin/readonly 两级，readonly 只读白名单，审计 actor=`svc:<名称>`）；插件 `platform/plugins/kabage-admin-tools` 注册 7 个工具（`user_list`/`user_quota_query`/`audit_query`/`user_create`/`user_update`/`user_reset_password`/`user_set_disabled`，raw `ctx.tools.register` + `inject:['tools']` + generic 呈现，token 经实例 env 注入，401/403/4xx/网络错转译中文，列表≤50 截断）。

## 八、本期验收口径

1. 管理台全部敏感操作（登录成败、成员/模型/供应商/实例/插件变更、审计导出、安全设置修改）在 `data/audit.jsonl` 留痕，控制台「安全与审计」页可筛选查询、可导出。
2. 连续错误密码触发锁定与 `auth.login_rate_limited` 事件；弱密码在设密/重置时被拒并有明确文案。
3. auditor 账号可登录管理台，能看总览/成员/审计，任何变更操作返回 403。
4. 配额按点数预扣/实结/返还：mock 上游验证 200/429/失败返还三条路径；倍率改动下一请求生效。
5. 存量行为不回归：现有 11 个成员端点、模型增删、插件投放、实例编排全部照常。
