# 落云宗企业平台 —— 角色与权限矩阵（阶段 0 交付物）

> 依据：`落云宗企业平台任务书.md`（v3）第 1 节已拍板决定 + 5 页 UI 设计图。
> 本文件回答两个问题：每个角色能做什么（操作矩阵）；5 页 UI 上每个可操作元素归谁（映射表）。
> 术语以任务书第 4 节为准，本文件不新增术语。

## 1. 角色集（MVP 裁剪为 3 个）

| 角色 | MVP 阶段 | 说明 |
|---|---|---|
| 平台管理员 | 阶段 1 起 | 企业 IT。账号、模型、实例、插件的全部管理操作。UI 截图中的「平台管理员」即此角色。 |
| 审计员 | 阶段 2+（先并入平台管理员） | 只读审计日志与用量元数据。MVP 由平台管理员兼任；触发拆分的条件：员工数 > 50，或企业合规要求职责分离。 |
| 员工 | 阶段 1 起 | 通过统一入口登录，使用自己的实例（会话、模型、插件、内容创作）。UI 截图中的「成员」即此角色。 |

OwnDsh 的五角色中「模型管理员 / 插件管理员」在 MVP 并入平台管理员（矩阵中标注，未来拆分不动表结构，只拆角色绑定）。

## 2. 操作矩阵（✓=允许，—=禁止）

| 操作（按 UI 页面分组） | 平台管理员 | 审计员 | 员工 |
|---|---|---|---|
| **成员与额度页** | | | |
| 查看成员列表与用量元数据 | ✓ | ✓ | —（仅本人） |
| 添加 / 编辑 / 删除账号 | ✓ | — | — |
| 冻结 / 禁用 / 启用账号 | ✓ | — | — |
| 调整额度（个人/部门） | ✓ | — | — |
| 重置密码 | ✓ | — | —（改本人密码走自助） |
| **模型与权限页** | | | |
| 上游提供方管理（增删改 BASEURL + 真实 Key） | ✓（写入/轮换；明文不可读回） | — | — |
| 模型目录管理（增删停改、计价倍率） | ✓ | — | — |
| 成员×模型授权矩阵勾选 | ✓ | — | — |
| **实例管理页** | | | |
| 查看实例列表（端口/pid/uid/目录/资源） | ✓ | ✓ | —（仅本人实例状态） |
| 启动 / 重启 / 停止实例 | ✓ | — | —（重启本人实例：阶段 4 后开放） |
| 同步配置 / 重发说明 | ✓ | — | — |
| 查看实例日志 | ✓ | — | — |
| 回收空闲实例 | ✓ | — | — |
| **插件管理页** | | | |
| 安装插件（npm / Git / tgz / 本地） | ✓ | — | — |
| 预检 / 下发（全局或定向）/ 回滚 / 删除 | ✓ | — | — |
| 实例×插件启用 / 停用 | ✓ | — | — |
| **审计与系统** | | | |
| 查看审计日志 | ✓ | ✓ | — |
| 系统状态页 | ✓ | ✓ | — |
| 企业设置（品牌 / 入口 / IdP 同步） | ✓ | — | — |
| **员工工作区** | | | |
| 创建 / 使用会话、选用已授权模型 | — | — | ✓ |
| 使用已下发插件（含内容创作工作站） | — | — | ✓ |
| 查看本人产物与内容日历 | — | — | ✓ |

## 3. 数据可见性矩阵（隐私边界，来自任务书决定 5）

| 数据 | 平台管理员 | 审计员 | 员工 |
|---|---|---|---|
| 本人会话正文 | — | — | ✓（仅本人） |
| 他人会话正文 | **—（永久禁止）** | — | — |
| 内容产物正文 / 内容日历条目正文 | **—（永久禁止）** | — | ✓（仅本人） |
| 用量元数据（谁/何时/模型/token 数） | ✓（全员） | ✓（全员） | ✓（仅本人） |
| 合规门禁结论（通过/不通过+时间） | ✓ | ✓ | ✓（本人） |
| 上游真实 Key 明文 | **—（无人可见，仅可写入/轮换）** | — | — |
| 实例配置 dump | ✓ | — | — |
| 审计日志 | ✓ | ✓ | — |

落点提醒：上表「—」由两条机制保证而非 UI 藏表格——Relay 与控制面**不存储、不转发**正文（任务书第 8 节的顶层 gate 机器验证）；实例会话日志只在实例本地 DSH_HOME。

## 4. 5 页 UI 可操作元素 → 术语 + 角色 + 阶段映射

约定：元素名取自截图原文；「两者」=平台管理员+审计员。

### 4.1 员工工作区（即员工实例的 Web UI）

| 元素 | 术语 | 角色 | 阶段 |
|---|---|---|---|
| 登录入口（企业品牌） | 账号 | 员工 | 阶段 2 |
| 新会话 / 会话列表 / 工作区分组 | 实例、session | 员工 | 阶段 1（结构）/ 阶段 2（登录） |
| 模型选择器（如 DeepSeek V4 Flash） | 授权集∩实际生效集 | 员工（只读可见） | 阶段 3 |
| 底部额度进度条 | 配额 | 员工（只读本人） | 阶段 4 |

### 4.2 成员与额度

| 元素 | 术语 | 角色 | 阶段 |
|---|---|---|---|
| 添加用户 / 编辑 / 删除 | 账号 | 平台管理员 | 阶段 2 |
| 部门列 / 角色列 | 账号.部门、角色 | 平台管理员（维护）、两者（查看） | 阶段 2 |
| 可见模型徽标 | 授权集 | 平台管理员（维护） | 阶段 3 |
| 本月额度进度条 / 调整额度 | 配额（硬停语义） | 平台管理员 | 阶段 4 |
| 停止 / 禁用 / 启用 / 冻结 | 账号.状态 | 平台管理员 | 阶段 2 |
| 重置密码 | 账号凭证 | 平台管理员 | 阶段 2 |
| 重启（实例操作入口） | 实例 | 平台管理员 | 阶段 1 |
| 最近活动列 | 用量元数据 | 两者 | 阶段 4 |
| 同步 IdP·待接入（置灰） | 非目标（接口预留） | — | 不做 |

### 4.3 模型与权限

| 元素 | 术语 | 角色 | 阶段 |
|---|---|---|---|
| 上游提供方表（BASEURL / 脱敏 KEY / 模型数） | Relay 上游 | 平台管理员 | 阶段 3 |
| 「真实 Key 只存在于 Relay 进程」注释 | 虚拟钥匙 / Relay 架构的 UI 表述 | — | 阶段 3 |
| 可用模型表（上下文/最大输出/倍率四列） | 模型目录、计价倍率 | 平台管理员 | 阶段 3–4 |
| 允许实例配置（管理员/普通员工标签） | 授权集 | 平台管理员 | 阶段 3 |
| 启用 / 停用 / 删除（模型行） | 模型目录.状态 | 平台管理员 | 阶段 3 |
| 成员×模型矩阵勾选（去掉勾立刻生效） | 授权集 | 平台管理员；生效机制=Relay per-request 校验 | 阶段 3–4 |

### 4.4 实例管理

| 元素 | 术语 | 角色 | 阶段 |
|---|---|---|---|
| 统计卡（实例数/需处理/异常/磁盘占用） | 实例、用量元数据 | 两者 | 阶段 1 |
| 状态列（已停止/运行中/需处理） | 实例.状态 | 两者 | 阶段 1 |
| 端口·进程·隔离列（:3181 / pid / uid 30000 / 隔离 id） | 实例（端口/pid/uid/DSH_HOME） | 两者 | 阶段 1 |
| 模型列（已授权 → 实例内渲染，含差异） | 授权集 vs 实际生效集 | 平台管理员 | 阶段 5 |
| 资源列（内存/磁盘） | 用量元数据 | 两者 | 阶段 1 |
| 运行时长·最后活动 | 用量元数据 | 两者 | 阶段 1 |
| 启动 / 重启 / 同步配置 / 重发说明 / 日志 / 删除 | 实例操作 | 平台管理员 | 阶段 1（重启/日志）、阶段 5（同步配置） |
| 回收空闲实例 | 实例回收（空闲判定=阶段 1 定义为手动，自动回收后续） | 平台管理员 | 阶段 1（手动） |
| 实例设置 | 实例.配置 | 平台管理员 | 阶段 5 |

### 4.5 插件管理

| 元素 | 术语 | 角色 | 阶段 |
|---|---|---|---|
| 统计卡（已启用/需要安装/待生效实例/磁盘占用） | 插件、暂存/生效 | 两者 | 阶段 6 |
| 安装插件（npm 包 / Git 仓库 / tgz / 本地） | 插件来源 | 平台管理员 | 阶段 6 |
| 平台默认列 / 暂存版本→待生效 | 暂存/生效两阶段 | 平台管理员 | 阶段 6 |
| 预检列（已预检） | 真启动预检 | 平台管理员 | 阶段 6 |
| 客户端列（客户端/内置于 DSH） | 插件来源形态 | 平台管理员 | 阶段 6 |
| 范围 / 预览 / 删除 | 下发（全局或定向）/ 回滚 | 平台管理员 | 阶段 6 |
| 实例×插件启停（已停止标签） | 实例.插件状态 | 平台管理员 | 阶段 6 |
| 试点行：@weibaohui/dsh-file-share → 首个真实试点改为内容创作工作站 | 试点受管插件 | 平台管理员 | 阶段 6 |

> 注：截图中插件市场的试点插件是 `dsh-file-share`；按任务书第 6 节，实际开发以仓库内**内容创作工作站**为首个受管插件试点，`dsh-file-share` 可作第二例验证非 UI 型插件。

## 5. 未在本矩阵中的操作

一律默认拒绝（deny by default）。新增 UI 元素时先入矩阵再实现——这是阶段 0 验收（每个可操作元素映射到唯一术语与角色）的持续生效条件。
