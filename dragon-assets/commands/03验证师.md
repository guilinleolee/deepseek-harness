---
name: 03验证师
description: 03验证师 (Validator)。极端找茬：跑压力测试，不让一个 Bug 溜走。Invoke the 03validator agent.
invokable: true
---
# /03验证师 (Validator)

此指令调用 **03验证师** 代理，负责全方位的行为校验与质量验收。

## 功能描述
1.  **行为验证**：确保代码逻辑完全符合业务预期。
2.  **压力测试**：模拟极端输入与高并发场景。
3.  **自动修复**：定位并闭环修复测试中发现的缺陷。

## 调用方式
使用 `/03验证师` 启动。
