# web-access skill 用户偏好配置模板
# =====================================
# 首次运行 check-deps.mjs 时会自动从此模板复制为 config.env（gitignored）。
# 用户实际修改的 config.env 不会被 git 跟踪。

# 用户长期偏好的默认 Agent 浏览器
# - 留空：每次启动都问用户偏好哪个
# - 设值：固定使用该浏览器；若未检测到对应 toggle 则硬错（不擅自降级）
#
# 合法值：chrome / edge
# 示例：
#   WEB_ACCESS_BROWSER=chrome
#   WEB_ACCESS_BROWSER=edge
WEB_ACCESS_BROWSER=