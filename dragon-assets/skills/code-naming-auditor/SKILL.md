---
license: UNKNOWN
name: code-naming-auditor
description: |
审计代码库命名一致性，确保遵循既定的术语和规范。
触发词: 命名审计、代码命名检查、术语一致性、变量命名规范。
使用场景: (1) 强制执行项目的"通用语言" (2) 识别方法/变量/参数命名偏差 (3) 长期维护项目/微服务架构命名统一。
author: github/cafe3310
adapted-by: Claude Code
version: 1.0.0
date: 2026-03-04
allowed-tools: - Read
- Write
- Edit
- Bash
- Grep
- Glob
triggers: ["code naming auditor", "代码命名审计 Skill (Code Naming Auditor)"]
---

# 代码命名审计 Skill (Code Naming Auditor)

本 Skill 指导 Agent 对代码库执行全面的审计，以确保其遵循一套明确定义的术语和命名规范。其核心目标是在整个项目中强制执行"通用语言"（Ubiquitous Language），从而提高代码的清晰度、一致性和可维护性。

## 触发条件

- 用户说"审计命名"、"检查代码命名一致性"
- 需要强制执行项目的"通用语言"
- 识别方法/变量/参数命名中的偏差

## 工作流 (Workflow)

### 第一步：建立术语表 (Establish the Glossary)

清晰、明确的术语表是所有代码审计工作的基础。

1. **检查现有术语表**: 在 `docs/` 或根目录中寻找名为 `glossary.md`、`terminology.md` 或类似的文件。
2. **与用户确认**: 如果找到了文档，需向用户展示并确认它是否是本次审计工作的"事实之源"（source of truth）。
3. **创建新术语表**: 如果项目中不存在术语表，应告知用户其重要性。术语表应精确定义项目核心的 **名词** (Nouns) 和 **动词** (Verbs) 的语义。

**在拥有一个清晰且双方同意的术语表之前，不应继续下一步。**

### 第二步：定义审计范围 (Define the Audit Scope)

1. **询问用户**: 提示用户指定他们希望审计的目录或文件。
2. **确认范围**: 在继续之前，与用户确认范围。

### 第三步：分析与报告 (Analyze and Report)

这是核心执行阶段。系统地分析在已定义范围内的每一个文件。

1. **读取文件**: 一次读取一个文件内容。
2. **分析命名**: 仔细检查文件中的编程命名：
   - 方法 / 函数名
   - 参数名
   - 变量名
   - 类型名 / 别名
3. **对照术语表**: 将每一个命名与术语表中的规则和定义进行比较。尤其要注意：
   - **动词错用**: 函数名是否在应该使用 `search` 的地方误用了 `find`？
   - **名词不一致**: 一个代表文件多行内容的 `string[]` 是否被命名为 `lines` 而不是术语表中定义的 `Content`？
   - **模糊性/歧义**: 是否存在像 `data`, `item`, `val`, `str` 这样的模糊命名？
4. **汇总偏差**: 为当前文件创建一个清单，列出所有已识别的命名偏差。
5. **提交报告**: 分析完成后，以清晰、结构化的格式向用户呈现汇总后的偏差与建议清单。

### 第四步：实施重构 (Refactor - 可选)

在提交报告后，Agent 可以主动提出应用这些修改建议。

1. **提议重构**: 询问用户："您希望我将这些命名修正应用到代码库中吗？"
2. **应用修改**: 如果用户同意，使用 Edit 工具系统地应用每一个修改建议。
3. **验证**: 重构完成后，运行项目的测试套件，以确保这些变更没有引入任何回归问题。

## 天龙引擎集成

### 与06审查师协同

此技能增强天龙引擎06审查师的代码质量审计能力:
- **命名一致性审计**: 新增专项审计维度
- **术语统一**: 强制执行项目通用语言
- **可维护性提升**: 减少认知负担

### 使用示例

```bash
# 触发技能
"审计这个项目的代码命名一致性"

# 指定范围
"检查 src/api/ 目录下的命名是否遵循术语表"
```

## 术语表模板

```markdown
# 项目术语表

## 名词 (Nouns)
- Content: 表示文件的多行内容
- Section: 表示文档的一个章节
- Line: 表示单行文本

## 动词 (Verbs)
- get: 获取单个对象
- list: 列出多个对象
- search: 搜索匹配的对象
- find: 查找特定条件下的对象
```